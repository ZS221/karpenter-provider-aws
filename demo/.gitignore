# Regenerated by ensure_kubeconfig() in perf/ab/config.sh; contains a live EKS
# API endpoint and cluster CA, so it must never be committed to the public fork.
perf/ab/kubeconfig
perf/ab/kubeconfig.tmp
